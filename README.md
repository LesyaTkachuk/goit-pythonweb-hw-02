# Homework: Docker basics

To create virtual environment and install all dependancies:

```bash
pip install -r requirements.txt
```

Run:

```bash
python main.py
```

To run an app and database into Docker containers:

```bash
docker-compose up
```
